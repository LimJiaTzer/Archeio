"""ocr_pipeline: image -> styled, editable docx.

Phase 1 (this package right now): text + tables, CPU-only.
Phase 3 (later): diagram/flowchart shape reconstruction, still CPU-only
except for the rare, optional VLM fallback.
"""
